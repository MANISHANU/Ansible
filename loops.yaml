- name : loops in ansible
  hosts : localhost 
  tasks :
  - name : loops 
    ansible.builtin.debug:
     msg : "Hi {{item}}"
    loop :
    - shell
    - ansible
    - linux 


- name : install packages 
  hosts : localhost
  become : yes 
  tasks :
  - name : loops 
  ansible.builtin.dnf:
  - name : "{{item}}"
  = state : latest
    msg: "{{item}} installed"
  loop:
  - ansible
  - nginx
  
